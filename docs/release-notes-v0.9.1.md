## Stream Media Board v0.9.1

### Dashboard — search in URL

- The **Search clips** field syncs to the `search` query parameter (e.g. `/?search=intro`).
- Save or share bookmarkable dashboard URLs with predefined filters.

### Upgrade from v0.9.0

Install over v0.9.0. No database or overlay URL changes. Refresh the dashboard after updating if it was already open.
