## Stream Media Board v0.36.1

### Fix

- Inclui o pacote `ws` no instalador Windows. Sem isso, o backend da v0.36.0 falhava ao iniciar (`Cannot find package 'ws'` / AdvSS WebSocket).

### Upgrade from v0.36.0

Instale o setup desta versão (ou use **Check for Updates**). Não há migração de banco.
