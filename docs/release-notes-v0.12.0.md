## Stream Media Board v0.12.0

### Dashboard and shell layout

- **Full-width only on the dashboard**: clip edit and layout areas pages use a centered `max-w-6xl` layout for easier reading.
- **Search toolbar**: removed the separate Clear button; use the native clear control on the search field.

### Upgrade from v0.11.0

Install over v0.11.0. No database or overlay URL changes. Refresh the app after updating if it was already open.
